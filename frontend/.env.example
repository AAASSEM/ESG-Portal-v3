# Backend Configuration
# Override the default Django backend port (8000) if needed
REACT_APP_BACKEND_PORT=8000

# For custom backend URLs in development
# REACT_APP_API_URL=http://localhost:8080

# Ngrok Configuration (if using ngrok for sharing)
# REACT_APP_NGROK_URL=https://abc123.ngrok-free.app